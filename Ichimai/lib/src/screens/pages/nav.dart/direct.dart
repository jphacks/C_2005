// 다이렉트 접속
// 채널을 입력 후 접속을 누르면 연결되는 방식
import 'package:flutter/material.dart';

class DirectConnectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(),
    );
  }
}
